name: Disabled - configure-pages

on: {}

# This workflow used to attempt to configure the Pages source via the REST API.
# Keep it disabled to avoid conflicting updates with the official 'deploy-pages.yml'.

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "configure-pages workflow intentionally disabled. Use repo Settings → Pages to configure source."
